#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webjs/bin/webjs" "$@"
  ret=$?
else 
  node  "$basedir/../webjs/bin/webjs" "$@"
  ret=$?
fi
exit $ret
