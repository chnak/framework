var lodash = require('lodash');var fs = require('fs');function beforeCreate(sender, entity){	entity.getType().setDefaultValues(entity.initData);	/*	var defaultValues=sender.defaultValues;	lodash.forEach(defaultValues,function(value,key){		var isFunc=lodash.isFunction(value);		if(!entity.initData[key])entity.initData[key]=isFunc?value():value;	})	*/	//console.log(sender.memberDefinitions.getPublicMappedProperties())}exports.loads=function(obj,listen,func){	lodash.forEach(obj,function(value,key){		value.addEventListener(listen, func);	})}exports.beforeCreate=beforeCreate;